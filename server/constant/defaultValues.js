const DEFAULT_PAGINATION = {
  LIMIT: 10,
  PAGE: 1,
};

const DEFAULT_SORT_BY_ORDER_BY = {
  MOVIES: { SORY_BY: "Title", ORDER_BY: "asc" },
};

module.exports = {
  DEFAULT_PAGINATION,
  DEFAULT_SORT_BY_ORDER_BY,
};
